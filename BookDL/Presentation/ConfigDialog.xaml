<Window x:Class="BookDL.Presentation.ConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookDL.Presentation"
        mc:Ignorable="d"
        Title="BookDLの設定" SizeToContent="WidthAndHeight">
    <Grid Width="400" Height="Auto" Margin="10,10,10,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0">出力ディレクトリ</TextBlock>
        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding OutputDirectory}"/>
            <Button Grid.Row="0" Grid.Column="1" Content="参照..."/>
        </Grid> 
        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Top">出力形式</TextBlock>
        <ListBox Grid.Row="1" Grid.Column="1" Width="100" Height="48" HorizontalAlignment="Left" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" Margin="10,0,10,0"/>
        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.Column="1" Content="OK" Command="{Binding OKCommand}"/>
            <Button Grid.Row="0" Grid.Column="2" Content="キャンセル" Command="{Binding CancelCommand}"/>
        </Grid>
    </Grid>
</Window>
